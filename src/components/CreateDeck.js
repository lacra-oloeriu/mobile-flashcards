import React from "react";
import { View, Text } from "react-native";

export const CreateDeck = ({ navigation }) => {
  return (
    <View>
      <Text>Create deck component</Text>
    </View>
  );
};
